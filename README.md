# Команди управління

### Запуск програми
Запускає веб-сервер на порту 5000:
`dotnet run --urls "http://localhost:5000"`

### Повне скидання програми
Виконує повний "зброс" даних, які були зроблені у програмі:
`rm tournaments.db*`
